version: 2
jobs:
  build:
    
    working_directory: ~/server/conference-buddy-parent

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:

      - checkout
      
      - run: mvn install
